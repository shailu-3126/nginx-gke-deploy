apiVersion: v1
kind: Service
metadata:
  name: mongodb-lb
  namespace: databases
spec:
  type: LoadBalancer
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  selector:
    app: mongodb
